name: flutter_app
description: Flutter-snap test app.
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  collection: ^1.15.0
  flutter:
    sdk: flutter
  window_manager: ^0.1.5

flutter:
  uses-material-design: true
